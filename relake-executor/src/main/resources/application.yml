spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
  kafka:
    bootstrap-servers: ${KAFKA_SERVERS:localhost:9092}
    consumer:
      group-id: relake-canal-consumer
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

canal:
  kafka:
    topic: ${CANAL_KAFKA_TOPIC:example}

datax:
  home: ${DATAX_HOME:/opt/datax}
  # Docker 模式下，DataX 容器内 localhost 应指向宿主机
  docker-host: ${DATAX_DOCKER_HOST:host.docker.internal}

flink:
  gateway:
    url: ${FLINK_GATEWAY_URL:http://localhost:8081}
